---
title: 스윕이 실행 중일 때 Python 파일을 수정하면 어떻게 되나요?
---

스윕이 실행 중일 때:

- 스윕에서 사용하는 `train.py` 스크립트가 변경되더라도, 스윕은 실행 시작 시점의 원래 `train.py`를 계속 사용합니다.
- 반면 `helper.py` 스크립트의 헬퍼 함수처럼 `train.py` 스크립트가 참조하는 파일이 변경되면, 스윕은 변경된 `helper.py`를 사용하기 시작합니다.
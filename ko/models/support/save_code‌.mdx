---
title: 코드는 어떻게 저장하나요?‌
---

`wandb.init`에서 `save_code=True`를 사용하여 실행을 시작하는 메인 스크립트나 노트북을 저장하세요. 실행의 모든 코드를 저장하려면 코드를 아티팩트로 버전 관리하세요. 다음 예제는 이 과정을 보여줍니다:

```python
code_artifact = wandb.Artifact(type="code")
code_artifact.add_file("./train.py")
wandb.log_artifact(code_artifact)
```
